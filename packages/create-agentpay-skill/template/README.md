# `__PROJECT_NAME__`

A paid Next.js Skill generated by AgentPayKit.

## Develop

Update `agentpay.skill.ts` with your publisher details and keep your business
implementation in `src/review-repository.ts`.

```bash
pnpm dev
```

The generated `SKILL.md` documents the local paid endpoint.

## Verify

```bash
pnpm verify
```
