# create-agentpay-skill

Create a new AgentPayKit paid Skill project from the community preview template.

Install:

```bash
pnpm create agentpay-skill@alpha my-paid-skill
```

The generated project targets Base Sepolia, fixed USDC pricing, synchronous
JSON requests, and manual wallet confirmation for each payment.
