{
  "name": "@agentpaykit/runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && wrangler deploy --dry-run --outdir dist-worker",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@agentpaykit/tsconfig": "workspace:*",
    "esbuild": "0.25.12",
    "miniflare": "4.20260714.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9",
    "wrangler": "4.110.0"
  },
  "dependencies": {
    "@agentpaykit/payment": "workspace:^",
    "@x402/core": "2.19.0",
    "@x402/evm": "2.19.0",
    "@x402/hono": "2.19.0",
    "hono": "4.12.31"
  }
}
