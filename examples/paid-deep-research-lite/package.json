{
  "name": "paid-deep-research-lite",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@agentpaykit/tsconfig": "workspace:*",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
